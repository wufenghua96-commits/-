# Hero 视频说明

首页使用 `hero-reel.mp4` 作为全屏背景视频。

- 本地路径：`public/media/hero-reel.mp4`
- 当前时长：约 10 秒
- 自动静音播放，不循环
- 只有完整刷新页面时重新播放
- 在站内进入项目详情再返回首页时，停留在视频最后一帧

后续替换视频时保持文件名不变即可。建议使用 1920×1080、H.264、8—15 秒、无声、文件小于 12 MB。
